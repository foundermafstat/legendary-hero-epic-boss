// Shared configuration constants
export const GAME_CONFIG = {
    // World settings
    WORLD_WIDTH: 3000,
    WORLD_HEIGHT: 3000,

    // Player settings
    PLAYER_RADIUS: 22, // Increased from 20
    PLAYER_HP: 100,

    // Mob settings  
    MOB_RADIUS: 20, // Increased from 18

    // Bullet settings
    BULLET_RADIUS: 3,

    // Wall config (for compatibility)
    WALL_MIN_SIZE: 60,
    WALL_MAX_SIZE: 200,

    // Visual settings
    FLOOR_COLOR: 0x2a2a2a,
    GRID_COLOR: 0x333333,
    TILE_SIZE: 64,
    FOG_ALPHA: 0.95,
};
